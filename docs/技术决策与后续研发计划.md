# 技术决策与后续研发计划

> 记录时间：2026-08-22 Day1 晚
> 决策人：浩哥

## 结论

1. **Day1 验收优先**：保持当前「文件加载 + 内存 n-gram 检索」方案，确保 22:00 演示稳定。
2. **验收后存档**：验收通过后打版本标签（如 `v0.1.0`），再继续后续研发。
3. **后续按小孙计划推进**：Day2 起逐步完成数据库升级与 RAG 质量提升。

## 数据库路线

| 阶段 | 方案 | 用途 |
|---|---|---|
| Day1 Demo | JSON 文件 + 内存 n-gram 检索 | 保证演示、无基础设施依赖 |
| Day2+ | PostgreSQL 16 + pgvector | 向量语义检索、多用户、审计留痕 |
| 最终私有化 | PostgreSQL 16 + pgvector | 生产交付固定标准 |

## Day2 后续计划（小孙）

- 补齐 `Repository` 抽象，使数据库/检索切换只改配置。
- PostgreSQL + pgvector 接入。
- 知识库文档切片 + embedding 向量化入库。
- 检索升级为向量召回 + Rerank。
- `/api/chat` 改为 SSE 流式输出，满足 TTFT ≤ 5s。
- 会话记录、审计日志持久化。
- 补齐 CI/CD（GitHub Actions）与分支策略（`main` 保护 + `dev` 开发）。


---

## 2026-08-28 更新：S05 交付总结与 S06 规划

### S05 已交付（v1.1.0, commit 28e0fb8）
- UI 视觉焕新 + 公文写作抽屉 A 方案
- RAG 检索链路稳定性：后端并发优化、nginx keepalive/gzip、前端 React.memo/流式节流
- cpolar HK 隧道迁移，固定域名 api.zqhelper.site
- 前端网络请求自动重试 + 消息列表性能优化
- P0 公文写作 query 透传修复、P1 SSE 401/Markdown 围栏修复
- 全量回归 55 项，P0/P1 全部关闭

### S06 规划（2026-08-29 ~ 09-01）

| 方向 | 内容 | 目标 |
|---|---|---|
| 知识库扩充 | 补充政策正文 + FAQ 问答对，覆盖社保/公积金/企业设立等高频场景 | 召回覆盖率提升 |
| 语义分块 | 500 字/块 + 100 字重叠，替代当前整段入库 | 检索精度提升 |
| 查询理解 | LLM query 改写（口语化 → 标准政务术语） | 意图识别准确率 |
| 香港服务器迁移 | 从 cpolar 隧道切换到香港云服务器直连公网 IP | TTFB 260ms → 50ms |
| 输入框聚焦 | 改用屏幕宽度 + React ref focus 方案，替代 CSS 媒体查询 | P2 收尾 |
| 脏数据清理 | 清理 knowledge_items 表 3 条测试脏数据 + 向量表同步 | 数据卫生 |

### S07-S08 长期规划
- 个性化推荐（用户画像 + 历史行为）
- 增量索引（文档更新不重建全量索引）
- BM25 全文检索 + 向量混合检索
- 引用溯源（GB/T 7714 格式）
- 私有化部署方案定稿（.env 模板 + 离线部署手册）
